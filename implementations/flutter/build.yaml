targets:
  $default:
    builders:
      drift_dev:
        options:
          # Map camelCase Dart getters to the shared SQL schema's snake_case
          # column names (is_done, sort_order, created_at, updated_at).
          case_from_dart_to_sql: snake_case
